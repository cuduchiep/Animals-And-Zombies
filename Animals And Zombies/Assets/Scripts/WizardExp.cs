using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WizardExp : MonoBehaviour
{

    // Use this for initialization
    void Start()
    {
        Destroy(gameObject, 0.75f);
    }

    // Update is called once per frame
    void FixedUpdate()
    {

    }
}
